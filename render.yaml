services:
  - type: web
    name: redeye-coin-scanner
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: GOOGLE_VISION_API_KEY
        sync: false
